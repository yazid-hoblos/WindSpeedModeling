model5 = lm(wv~.,df[-1])
summary(model5)


## ----------------------------------------------------------------------------------------------------------------------------------------------------------------------
model6 = lm(wv~.,df[-1][-13])
summary(model7)


## ----------------------------------------------------------------------------------------------------------------------------------------------------------------------
model7 = lm(wv~.,df[-1][-14])
summary(model6)


## ----------------------------------------------------------------------------------------------------------------------------------------------------------------------
model_check = lm(wv~wvMax+wd,df)
summary(model_check)
model_check = lm(wv~wvMax,df)
summary(model_check)


## ----------------------------------------------------------------------------------------------------------------------------------------------------------------------
plot(y=df$wv,x=df$wvMax,col='blue')
abline(model,col='red')


## ----------------------------------------------------------------------------------------------------------------------------------------------------------------------
mean(summary(model)$residuals^2)


## ----------------------------------------------------------------------------------------------------------------------------------------------------------------------
d = data.frame(pred=predict(model),actual=df$wv)
mean((d$actual-d$pred)^2)


## ----------------------------------------------------------------------------------------------------------------------------------------------------------------------
ggplot(data=df)+
  geom_point(mapping=aes(x=wv,y=wvMax,color=wv))+
  geom_smooth(mapping=aes(x=wv,y=wvMax))


## ----------------------------------------------------------------------------------------------------------------------------------------------------------------------
summary(model7)
summary(model1)
model8 = lm(wv~pressure+I(rh^2),df)
summary(model8)